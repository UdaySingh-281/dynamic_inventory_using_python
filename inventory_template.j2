{% for group, hosts in inventory.items() %}
[{% if group != 'ungrouped' %}{{ group }}{% endif %}]
{% for host, key_path in hosts %}
{{ host }} ansible_user=ubuntu ansible_ssh_private_key_file={{ key_path }}
{% endfor %}
{% endfor %}
